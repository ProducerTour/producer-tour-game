# Disable workspace features to prevent npm from installing parent workspace dependencies
# This ensures Render only installs backend dependencies, not frontend
workspaces=false
legacy-peer-deps=true
